import { AdminShowDirective } from './admin-show.directive';

describe('AdminShowDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminShowDirective();
    expect(directive).toBeTruthy();
  });
});
