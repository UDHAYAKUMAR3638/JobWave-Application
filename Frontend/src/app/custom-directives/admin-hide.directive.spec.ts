import { AdminHideDirective } from './admin-hide.directive';

describe('AdminHideDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminHideDirective();
    expect(directive).toBeTruthy();
  });
});
